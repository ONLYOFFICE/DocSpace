import React from "react";
import { StyledTableRow } from "./StyledTableContainer";

const TableRow = (props) => {
  return <StyledTableRow className="table-container_row" {...props} />;
};

export default TableRow;
