import styled from "styled-components";

const StyledSettings = styled.div`
  display: grid;
  grid-gap: 19px;

  .toggle-btn {
    position: relative;
  }

  .heading {
    margin-bottom: 1px;
    margin-top: 26px;
  }

  .toggle-button-text {
    margin-top: -1px;
  }
`;

export default StyledSettings;
