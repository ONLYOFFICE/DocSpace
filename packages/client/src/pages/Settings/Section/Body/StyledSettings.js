import styled from "styled-components";

const StyledSettings = styled.div`
  margin-top: 15px;
  width: 100%;

  display: grid;
  grid-gap: 19px;

  .toggle-btn {
    position: relative;
  }

  .heading {
    margin-bottom: -2px;
    margin-top: 3px;
  }

  .toggle-button-text {
    margin-top: -1px;
  }
`;

export default StyledSettings;
