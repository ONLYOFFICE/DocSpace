import React from 'react';
import { Badge } from '@docspace/components';

export const SuccessBadge = ({ label }) => (
  <Badge backgroundColor="#2DB4821A" color="#2DB482" isHovered label={label} noHover />
);
