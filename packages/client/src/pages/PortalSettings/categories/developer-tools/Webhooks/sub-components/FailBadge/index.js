import React from 'react';
import { Badge } from '@docspace/components';

export const FailBadge = ({ label }) => {
  return <Badge backgroundColor="#F21C0E1A" color="#F21C0E" isHovered label={label} noHover />;
};
