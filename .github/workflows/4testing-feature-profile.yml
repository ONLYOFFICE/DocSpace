name: 4testing multiarch-build

on:
  push:
    branches: 
      - 'feature/docker-build-profiles'
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: [linux/amd64]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build 4testing
        run: |
          pwd
          ls -l
          export DOCKER_TAG="dev-profile"
          export REPO="onlyoffice"
          export DOCKER_IMAGE_PREFIX="4testing-docspace"
          export DOCKERFILE="./build/install/docker/Dockerfile"

          docker buildx bake -f ./build/install/docker/build.yml \
          --set *.platform=linux/amd64 \
          --print

          docker buildx bake -f ./build/install/docker/build.yml \
          --set *.platform=linux/amd64 \
          --push
        shell: bash
